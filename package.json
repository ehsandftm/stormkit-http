{
  "name": "stormkit-relay-service",
  "version": "1.0.0",
  "description": "Stormkit API relay service",
  "private": true,
  "scripts": {
    "build": "echo build-ok",
    "start": "echo stormkit-functions"
  },
  "engines": {
    "node": ">=18"
  }
}
