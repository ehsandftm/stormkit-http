{
  "name": "stormkit-relay-service",
  "version": "1.0.0",
  "description": "Stormkit API relay service",
  "private": true,
  "scripts": {
    "start": "node -e \"console.log('This project runs as Stormkit API Functions (no express listen).')\""
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  }
}
