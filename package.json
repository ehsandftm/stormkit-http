{
  "name": "stormkit-http",
  "version": "1.0.0",
  "private": true,
  "description": "Stormkit HTTP relay",
  "scripts": {
    "build": "echo build-ok",
    "start": "echo stormkit-functions"
  },
  "dependencies": {
    "node-fetch": "2.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}
